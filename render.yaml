services:
  - type: web
    name: botnest-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --timeout 0
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
